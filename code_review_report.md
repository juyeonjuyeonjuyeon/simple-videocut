# SimpleCut 전체 코드 검수 보고서

## 결론

현재 코드는 React UI, Zustand 편집 상태, 시간 계산, 프로젝트 저장, 미디어 판별, FFmpeg 내보내기가 역할별로 나뉘어 있으며 배포 가능한 상태다. 전체 자동 검사와 실제 브라우저 내보내기가 통과했다. 치명적인 누락이나 동일 기능의 충돌 구현은 발견되지 않았다.

## 이번에 바로 개선한 내용

- 파일 드롭과 `+` 버튼의 중복 분류 로직을 하나로 통합했다.
- 미디어 파일 수·개별 크기·프로젝트 총용량을 브라우저 디코딩 전에 제한했다.
- 가져온 프로젝트의 전체 구조, 값 범위, 중복 ID, 끊어진 미디어 연결, 반복을 포함한 전체 길이를 검증한다.
- 자동저장 프로젝트도 복원 전에 같은 구조와 미디어 연결을 검증한다.
- 공유용 프로젝트 생성 시 과도한 base64 메모리 사용을 사전에 차단한다.
- ID 생성은 지원 브라우저에서 `crypto.randomUUID()`를 사용한다.
- 서비스 워커 캐시 버전을 갱신해 이전 배포 자산을 정리한다.
- 프로젝트 창에 남아 있던 문자·이모티콘 버튼을 통일된 SVG 아이콘으로 교체하고 저장·삭제·내보내기 오류가 사용자에게 누락되지 않도록 했다.
- CSP, Referrer Policy, GitHub Actions SHA 고정을 적용했다.

## 구조상 잘 되어 있는 부분

- 시간 계산은 `utils/time.ts`, 파일 판별은 `utils/media.ts`, 저장 복원은 `utils/project.ts`, 변환은 `ffmpeg/exporter.ts`에 분리되어 있다.
- 미디어 원본은 서버로 전송하지 않고 브라우저와 IndexedDB 안에서만 처리한다.
- Object URL은 삭제·교체·내보내기 종료 경로에서 회수하며, 되돌리기에 필요한 URL은 즉시 폐기하지 않는다.
- FFmpeg 실패 시 엔진을 종료해 손상된 가상 파일 시스템과 메모리를 다음 작업에 넘기지 않는다.
- 개발 환경에서만 디버그 상태를 노출하고, 위험한 HTML 삽입이나 동적 코드 실행을 사용하지 않는다.

## 남은 개선 후보

1. `Preview.tsx`, `Timeline.tsx`, `Inspector.tsx`, `exporter.ts`는 각각 500~750줄이다. 현재 기능 경계는 유지되지만 다음 대형 기능을 추가하기 전 상호작용 훅·트랙 행·FFmpeg 입력 준비 단위로 분리하면 변경 위험이 낮아진다.
2. 메인 고해상도/HEVC 영상은 선행 정규화하지만 고해상도 비디오 오버레이와 배경은 같은 정규화 경로를 아직 공유하지 않는다. 해당 레이어를 많이 사용하는 4K 프로젝트는 메모리 압박이 남을 수 있다.
3. 메인 클립 반복은 반복 횟수만큼 FFmpeg 입력 스트림을 확장한다. 매우 큰 반복값은 현재 6시간 총 길이 제한으로 완화되지만, 장기적으로 `stream_loop` 기반 단일 입력으로 바꾸면 효율이 좋아진다.
4. 자동저장 원본의 보존 기간과 “모든 로컬 데이터 지우기” 설정은 개인정보 관리 기능으로 추가할 가치가 있다.
5. Google Fonts를 자체 호스팅하면 제3자 요청을 완전히 제거할 수 있다.

## 검증 결과

- 단위·회귀 테스트: 3개 파일, 11개 테스트 통과
- ESLint: 통과
- TypeScript 및 Vite 배포 빌드: 통과
- 실제 브라우저: CSP 적용 상태에서 앱 로드 오류 없음
- 실제 FFmpeg.wasm: HEVC 테스트 영상 MP4 내보내기 완료, 진행률 변화 및 완료 파일 생성 확인
